class ArticleSize {
  String size;
  int amount;

  ArticleSize({required this.size, required this.amount});
}

class ArticleSizeRepresentation {
  ArticleSize size;
  bool isSelected;

  ArticleSizeRepresentation({required this.size, required this.isSelected});
}
